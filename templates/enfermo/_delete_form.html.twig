<form method="post" action="{{ path('app_enfermo_delete', {'inscripcion': enfermo.inscripcion}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfermo.inscripcion) }}">
    <button class="btn">Delete</button>
</form>
