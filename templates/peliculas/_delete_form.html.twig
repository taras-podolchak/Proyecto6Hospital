<form method="post" action="{{ path('app_peliculas_delete', {'idpelicula': pelicula.idpelicula}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pelicula.idpelicula) }}">
    <button class="btn">Delete</button>
</form>
